import { FilmDurationPipe } from './film-duration.pipe';

describe('FilmDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
